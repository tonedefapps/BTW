package com.tonedefapps.btw.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────
val Ink   = Color(0xFF1C4A60)  // primary background
val Depth = Color(0xFF3A85A8)  // interactive / secondary
val Sky   = Color(0xFF7BB8D4)  // accent, borders, sublabels
val Air   = Color(0xFFEAF4FB)  // primary text on dark
val Sand  = Color(0xFFC9B49A)  // rider/pet names
val Dawn  = Color(0xFFF0E9E1)  // warm light surface

// ── Semantic colors ───────────────────────────────────────────────────────────
val AlertRed  = Color(0xFFE05555)  // alert / danger states
val SafeGreen = Color(0xFF4CAF82)  // safe / confirmed states
val WarnAmber = Color(0xFFF5A623)  // caution / pending states
